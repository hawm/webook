<p style="color: green"><%= notice %></p>

<div>
<h1 class='d-inline'>Books</h1>
<%= link_to "Upload", new_book_path, class: "btn btn-primary float-end" if current_user&.is_admin %>
</div>
<div id="books">
  <div class='row row-cols-5 g-4'>
  <% @books.each do |book| %>
    <div class='col'>
    <%= render book, simple: true %>
    </div>
  <% end %>
  </div>
</div>

